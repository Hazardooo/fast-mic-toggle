# Fast Mic Toggle

### Инструмент для автоматического восстановления работы неисправного микрофона в Windows через быстрое переключение аудиоустройств.

---

# 💡 Описание проблемы

Некоторые звуковые карты или USB-микрофоны в Windows имеют специфическую проблему: после запуска системы или выхода из
режима сна устройство помечается как активное, но не передает звук (становится "глухим") до тех пор, пока вы не
переключите его вручную на другое устройство, а затем обратно.

**Fast Mic Toggle** решает эту проблему, выполняя необходимое переключение автоматически, используя простой скрипт и
сохраняя настройки в реестре Windows.

---

# ✨ Принцип работы

Приложение выполняет быструю, автоматизированную операцию:

- Читает сохраненные индексы микрофонов (Основной и Временный).
- На короткое время (1 секунда) устанавливает Временный микрофон как устройство связи по умолчанию.
- Возвращает в качестве устройства связи по умолчанию ваш Основной микрофон.

Это быстрое переключение инициирует внутреннюю "перезагрузку" аудиодрайвера, после чего ваш Основной микрофон начинает
нормально работать в таких приложениях, как Discord или Zoom.

---

# 🚀 Установка

## 1. Как готовое приложение (.exe)

### Если вы скачали приложение в виде готового .exe файла:

1. Скачайте и распакуйте архив.
2. Запустите fast-mic-toggle.exe.

**Важно: Исполняемый файл (fast-mic-toggle.exe) не будет работать без папки _internal, которая содержит все необходимые
DLL-зависимости.**

## 2. Как Python-пакет (если вы разработчик)

`pip install -r requirements.txt`

`cd .\app\`

`python main.py`

# ⚙️ Использование (Настройка)

### Приложение запускается в режиме консольного меню:

```
--- fast-mic-toggle ---
1) Get a list of microphones
2) Create a fast toggle
3) Fast toggle
4) Delete config
0) Exit
```

## Шаг 1: Получение списка микрофонов (Пункт 1)

Запустите приложение и выберите пункт `1`, чтобы увидеть все доступные записывающие устройства и их системные индексы.

```
Select an option: 1
Your connected microphones:
{'Index': 5, ..., 'Name': 'Линия (Xonar U7 MKII)', ...}
{'Index': 7, ..., 'Name': 'Микрофон (Xonar U7 MKII)', ...}
```

- Определите свой основной микрофон: Тот, который вы хотите исправить (например, 7).
- Определите временный микрофон: Любой другой записывающий индекс, который будет использоваться для переключения (
  например, 5).

## Шаг 2: Создание конфигурации (Пункт 2)

Выберите пункт `2` для сохранения индексов. Конфигурация будет сохранена в реестре Windows (по пути
`HKEY_CURRENT_USER\Software\fast-mic-toggle`).

Введите два индекса через пробел:

- Основной микрофон (который нужно починить).
- Временный микрофон (через который будет происходить переключение).

### Пример:

```
Select an option: 2
Specify the selected microphone indexes (e.g., '1 2') or 'b' to go back: 7 5
✅ Config created successfully!
```

## Шаг 3: Быстрое переключение (Пункт 3)

После настройки вы можете в любой момент выполнить переключение, выбрав пункт `3`.

```
Select an option: 3
✅ Microphone toggled successfully!
```

Теперь ваш Основной микрофон (Индекс 7) должен быть полностью рабочим.

**Важное замечание: Перед выполнением переключения убедитесь, что вы вышли из всех приложений, которые могут
использовать микрофон (Discord, Zoom, игры), так как смена активного устройства связи может быть некорректно обработана
запущенными программами.**

---

## Удаление конфигурации (Пункт 4)

Если вы больше не нуждаетесь в инструменте, вы можете удалить сохраненные индексы из реестра:

```
Select an option: 4
✅ Config deleted.
```

---

# 💻 Интеграция с автозапуском Windows

Чтобы решить проблему полностью, вы можете настроить запуск fast-mic-toggle.exe при старте системы.

1. Убедитесь, что вы успешно настроили конфиг (Шаг 2).
2. Создайте ярлык для файла fast-mic-toggle.exe.
3. Откройте свойства ярлыка.
4. В поле "Объект" после полного пути к вашему .exe добавьте цифру 3 (чтобы приложение выполнило только пункт "Fast
   toggle" и завершило работу). Пример: "C:\Путь\К\Вашему\fast-mic-toggle.exe" 3
5. Поместите этот ярлык в папку автозагрузки Windows (shell:startup).

# 🗑️ Удаление

Просто удалите папку `fast-mic-toggle` и, при желании, используйте пункт `4` меню для очистки реестра.